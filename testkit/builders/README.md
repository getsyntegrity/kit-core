# builders

Fluent builders for test entities (e.g. TenantContext, QueryOptions) can be added here when they reduce duplication across specs. Prefer simple fixture functions in `fixtures/` when sufficient.
